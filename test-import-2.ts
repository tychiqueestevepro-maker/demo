import { PrismaClient } from '@prisma/client'; import { importTargetsFromCsv } from './lib/services/target-service'; const prisma = new PrismaClient(); async function test() { try { const user = await prisma.user.findFirst(); const campaign = await prisma.campaign.findFirst({ where: { userId: user.id } }); await importTargetsFromCsv(user.id, campaign.id, 'name,email\nLucas,lucas@example.com'); console.log('success'); } catch(e) { console.error(e); } finally { await prisma.(); } } test();  
